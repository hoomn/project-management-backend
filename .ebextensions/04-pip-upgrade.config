# Upgrade pip inside the Python virtual environment during EC2 provisioning
# Pre-deployment system commands
 
commands:
  01_pip_upgrade:
    command: "source /var/app/venv/*/bin/activate && pip install --upgrade pip"
